apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  labels:
    app: api
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: "{{ .Values.image.api.repository }}:{{ .Values.image.api.tag }}"
          imagePullPolicy: {{ .Values.image.api.pullPolicy }}
          ports:
            - containerPort: 3000
              name: http
            - containerPort: 3001
              name: websocket
          env:
            - name: DEFAULT_ADMIN__USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "seethrough.fullname" . }}-auth
                  key: admin-username
            - name: DEFAULT_ADMIN__PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "seethrough.fullname" . }}-auth
                  key: admin-password
            - name: DATABASE__HOST
              value: {{ .Values.storage.postgres.host | quote }}
            - name: DATABASE__DATABASE
              value: {{ .Values.storage.postgres.db | quote }}
            - name: DATABASE__USERNAME
              value: {{ .Values.storage.postgres.user | quote }}
            - name: DATABASE__PASSWORD
              value: {{ .Values.storage.postgres.password | quote }}
            - name: REDIS__HOST
              value: {{ .Values.storage.redis.host | quote }}
            - name: REDIS__PORT
              value: {{ .Values.storage.redis.port | quote }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "seethrough.fullname" . }}-auth
                  key: jwt-secret
            - name: LOG_LEVEL
              value: {{ .Values.config.logLevel | quote }}
          resources:
            {{- toYaml .Values.resources.api | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  type: {{ .Values.service.api.type }}
  ports:
    - port: 3000
      targetPort: 3000
      name: http
    - port: 3001
      targetPort: 3001
      name: websocket
  selector:
    app: api
